export default function Card() {
  return (
    <>
      <h1>
        {CardRankCount}{" "}
        <a href={linkTo-- > CompanyName - Router}>{CompanyName}</a>
      </h1>
      <div className="cardTag&Loc">
        <span>{tags}</span>
        <span>{Location}</span>
      </div>
    </>
  );
}
